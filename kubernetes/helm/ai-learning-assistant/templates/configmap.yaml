apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ai-learning-assistant.fullname" . }}-config
  labels:
    {{- include "ai-learning-assistant.labels" . | nindent 4 }}
data:
  PORT: "8000"
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  CORS_ORIGINS: "https://{{ .Values.domainName }}"
