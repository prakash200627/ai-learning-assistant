# AI Learning Assistant Frontend

This frontend now lives directly under `frontend/`.

## Run locally

```powershell
cd frontend
npm install
npm run dev
```

## Build

```powershell
cd frontend
npm run build
```

## Notes

- The app uses Vite, React, Tailwind CSS v4, and React Router.
- Shared layout and UI styles live in `src/index.css`.
